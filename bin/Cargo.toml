[package]
name = "bin"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
env_logger = { workspace = true }

window = { package = "window", path = "../crates/window" }
engine = { package = "engine", path = "../crates/engine" }
