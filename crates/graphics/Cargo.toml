[package]
name = "graphics"
version = "0.1.0"
edition = "2024"

[dependencies]
wgpu = { workspace = true }

window = { package = "window", path = "../window" }
types = { package = "types", path = "../types" }
